-- change auto dotfiles update to symlink

nvim:
  plugins:
    harpoon 
    wokwi (or equivalent)

  keybinds:
    {done} add C-w to esc s replace
    change replace to replace anything highlighted

  behaviour:
    stop tab menu when not absolute match
      (eg so np does not autocomplete to numpy)

  appearance:
    both relative and absolute line numbers

zsh:
  add some LLM to zsh
  LaTeX compiler
  tmux?
